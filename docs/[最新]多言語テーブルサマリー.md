# 多言語対応: テーブル別変更一覧（最終版）

**作成日**: 2025-11-21
**最終更新**: 2025-11-21
**ステータス**: 確定版

---

## 変更対象テーブル一覧

| # | テーブル名 | カテゴリ | 優先度 | 多言語カラム数 | 翻訳方式 |
|---|-----------|---------|--------|--------------|---------|
| 1 | cards | マスターデータ | 🔴 高 | 2 | 管理者作成時・全自動翻訳 |
| 2 | packs | マスターデータ | 🔴 高 | 1 | 管理者作成時・全自動翻訳 |
| 3 | deck_pages | 管理者コンテンツ | 🔴 高 | 23 | 管理者作成時・全自動翻訳 |
| 4 | info_pages | 管理者コンテンツ | 🔴 高 | 23 | 管理者作成時・全自動翻訳 |
| 5 | info_articles | 管理者コンテンツ | 🔴 高 | 7 | 管理者作成時・全自動翻訳 |
| 6 | info_article_blocks | 管理者コンテンツ | 🔴 高 | 1 | 管理者作成時・全自動翻訳 |
| 7 | tournaments | 管理者コンテンツ | 🟡 中 | 2 | 管理者作成時・全自動翻訳 |
| 8 | deck_notifications | 通知 | 🟡 中 | 1 | テンプレート自動生成 |
| 9 | trade_notifications | 通知 | 🟡 中 | 1 | テンプレート自動生成 |
| 10 | decks | ユーザーコンテンツ | 🟢 低 | 2 + status | バックグラウンド翻訳 |
| 11 | trade_posts | ユーザーコンテンツ | 🟢 低 | 4 + status | バックグラウンド翻訳 |
| 12 | trade_owned_list | ユーザーコンテンツ | 🟢 低 | 1 + status | バックグラウンド翻訳 |
| 13 | user_collages | ユーザーコンテンツ | 🟢 低 | 2 + status | バックグラウンド翻訳 |
| 14 | trade_comments | ユーザーコンテンツ | ⚪ 特殊 | 0 | **都度翻訳（オンデマンド）** |
| 15 | users | ユーザー管理 | - | 0 | **変更不要** |

**新規テーブル**: translation_cache, translation_jobs

**合計多言語カラム数**: 69個

---

## 1. cards（カードマスターデータ）

### 追加カラム

| 新カラム名 | 型 | NOT NULL | デフォルト | 説明 |
|-----------|-----|---------|-----------|------|
| name_multilingual | JSONB | ✅ | `'{"ja":""}'::jsonb` | カード名（全言語） |
| image_url_multilingual | JSONB | ✅ | `'{}'::jsonb` | カード画像URL（言語・地域別） |

### 管理者サイトでの対応

- ✅ 作成・更新時に**全言語を同時に入力または自動翻訳**
- ✅ 保存時にAWS Translate APIで自動翻訳してJSONBに格納
- ✅ 各言語のカード画像URLを個別に設定可能

### 既存データ変換

```sql
UPDATE cards SET
    name_multilingual = jsonb_build_object('ja', name),
    image_url_multilingual = jsonb_build_object(
        'ja', image_url,
        'en', image_url,
        'zh-CN', image_url,
        'zh-TW', image_url,
        'ko', image_url
    );
```

### インデックス

- `idx_cards_name_multilingual` (GIN)
- `idx_cards_image_url_multilingual` (GIN)

### 注意事項

- ❌ `poke2_name` - 廃止予定のため対応不要

---

## 2. packs（パックマスターデータ）

### 追加カラム

| 新カラム名 | 型 | NOT NULL | デフォルト | 説明 |
|-----------|-----|---------|-----------|------|
| name_multilingual | JSONB | ✅ | `'{"ja":""}'::jsonb` | パック名（全言語） |

### 管理者サイトでの対応

- ✅ 作成・更新時に全言語を同時に自動翻訳

### 既存データ変換

```sql
UPDATE packs SET name_multilingual = jsonb_build_object('ja', name);
```

### インデックス

- `idx_packs_name_multilingual` (GIN)

---

## 3. deck_pages（デッキガイド）

### 追加カラム（23個）

| 新カラム名 | 型 | 既存カラム | 説明 |
|-----------|-----|----------|------|
| title_multilingual | JSONB | title | タイトル |
| thumbnail_image_url_multilingual | JSONB | thumbnail_image_url | サムネイル画像URL |
| thumbnail_alt_multilingual | JSONB | thumbnail_alt | サムネイルalt |
| deck_badge_multilingual | JSONB | deck_badge | デッキバッジ |
| section1_title_multilingual | JSONB | section1_title | セクション1タイトル |
| deck_name_multilingual | JSONB | deck_name | デッキ名 |
| energy_image_url_multilingual | JSONB | energy_image_url | エネルギー画像URL |
| deck_cards_multilingual | JSONB | deck_cards | デッキカード一覧（JSONB） |
| deck_description_multilingual | JSONB | deck_description | デッキ説明 |
| evaluation_title_multilingual | JSONB | evaluation_title | 評価タイトル |
| tier_rank_multilingual | JSONB | tier_rank | ティアランク |
| tier_name_multilingual | JSONB | tier_name | ティア名 |
| tier_descriptions_multilingual | JSONB | tier_descriptions | ティア説明（配列） |
| stat_accessibility_multilingual | JSONB | stat_accessibility | 入手難易度（数値＋説明テキスト） |
| stat_speed_multilingual | JSONB | stat_speed | スピード（数値＋説明テキスト） |
| stat_power_multilingual | JSONB | stat_power | パワー（数値＋説明テキスト） |
| stat_durability_multilingual | JSONB | stat_durability | 耐久力（数値＋説明テキスト） |
| stat_stability_multilingual | JSONB | stat_stability | 安定性（数値＋説明テキスト） |
| section2_title_multilingual | JSONB | section2_title | セクション2タイトル |
| strengths_weaknesses_details_multilingual | JSONB | strengths_weaknesses_details | 強み・弱み詳細（JSONB） |
| section3_title_multilingual | JSONB | section3_title | セクション3タイトル |
| how_to_play_list_multilingual | JSONB | how_to_play_list | 使い方リスト（配列） |
| how_to_play_steps_multilingual | JSONB | how_to_play_steps | 使い方手順（JSONB） |

### 管理者サイトでの対応

- ✅ 作成・更新時に**全フィールドを全言語で入力または自動翻訳**
- ✅ JSONBフィールド（deck_cards, strengths_weaknesses_details, how_to_play_steps）の中身も全て翻訳

### 多言語化不要なカラム

- ❌ `category` - ENUM（i18nで翻訳）
- ❌ `is_published` - ブール値
- ❌ `view_count`, `like_count`, `favorite_count`, `eval_value`, `eval_count` - 統計数値
- ❌ `created_at`, `updated_at`, `last_updated` - 日時

### インデックス

- `idx_deck_pages_title_multilingual` (GIN)
- `idx_deck_pages_deck_cards_multilingual` (GIN)
- `idx_deck_pages_strengths_multilingual` (GIN)
- `idx_deck_pages_howto_multilingual` (GIN)

### JSONB構造の多言語化例

#### 1. deck_cards_multilingual（デッキカード一覧）

**既存構造**（日本語のみ）:
```json
[
  {
    "card_id": 4762,
    "pack_name": "未知なる水域",
    "card_count": 2,
    "display_order": 0
  }
]
```

**多言語化後**:
```json
{
  "ja": [
    {"card_id": 4762, "pack_name": "未知なる水域", "card_count": 2, "display_order": 0}
  ],
  "en": [
    {"card_id": 4762, "pack_name": "Unknown Depths", "card_count": 2, "display_order": 0}
  ],
  "zh-CN": [
    {"card_id": 4762, "pack_name": "未知水域", "card_count": 2, "display_order": 0}
  ],
  "zh-TW": [
    {"card_id": 4762, "pack_name": "未知水域", "card_count": 2, "display_order": 0}
  ],
  "ko": [
    {"card_id": 4762, "pack_name": "미지의 수역", "card_count": 2, "display_order": 0}
  ]
}
```

**翻訳対象キー**: `pack_name`のみ  
**翻訳不要キー**: `card_id`, `card_count`, `display_order`（数値・順序）

---

#### 2. strengths_weaknesses_details_multilingual（強み・弱み詳細）

**既存構造**（日本語のみ）:
```json
[
  {
    "title": "ベンチを並べて火力を出す",
    "image_urls": ["https://..."],
    "description": "<p>スイクンexはベンチのポケモンが多いほど...</p>",
    "display_order": 1
  }
]
```

**多言語化後**:
```json
{
  "ja": [
    {
      "title": "ベンチを並べて火力を出す",
      "image_urls": ["https://..."],
      "description": "<p>スイクンexはベンチのポケモンが多いほど...</p>",
      "display_order": 1
    }
  ],
  "en": [
    {
      "title": "Build your bench for power",
      "image_urls": ["https://..."],
      "description": "<p>Suicune ex's power increases with more benched Pokémon...</p>",
      "display_order": 1
    }
  ]
}
```

**翻訳対象キー**: `title`, `description`  
**翻訳不要キー**: `image_urls`, `display_order`

---

#### 3. how_to_play_list_multilingual（使い方リスト）

**既存構造**（日本語のみ）:
```json
{
  "序盤: ベンチ展開",
  "中盤: ナッシーexで制圧",
  "終盤: 入れ替えで粘り勝ち"
}
```

**データ型**: PostgreSQL配列（array）ではなくJSONBのkey-valueペア  

**多言語化後**:
```json
{
  "ja": {
    "序盤: ベンチ展開",
    "中盤: ナッシーexで制圧",
    "終盤: 入れ替えで粘り勝ち"
  },
  "en": {
    "Early game: Build your bench",
    "Mid game: Dominate with Exeggutor ex",
    "Late game: Win with switch stalling"
  }
}
```

**翻訳対象**: 全てのvalue（配列の各要素）

---

#### 4. stat_*_multilingual（ステータス値＋説明）

**既存構造**（数値のみの場合を想定）:
```json
3
```

**多言語化後**（数値＋ラベル＋説明）:
```json
{
  "ja": {
    "value": 3,
    "label": "入手しやすい",
    "description": "基本パックのカードで構成されており、無課金でも組める"
  },
  "en": {
    "value": 3,
    "label": "Easy to get",
    "description": "Composed of basic pack cards, buildable without spending"
  },
  "zh-CN": {
    "value": 3,
    "label": "容易获得",
    "description": "由基础卡包卡牌组成,免费玩家也能组建"
  },
  "zh-TW": {
    "value": 3,
    "label": "容易獲得",
    "description": "由基礎卡包卡牌組成,免費玩家也能組建"
  },
  "ko": {
    "value": 3,
    "label": "획득 쉬움",
    "description": "기본 팩 카드로 구성되어 무과금으로도 조합 가능"
  }
}
```

**翻訳対象キー**: `label`, `description`  
**翻訳不要キー**: `value`（数値）

---

## 4. info_pages（記事ページ）

### 追加カラム

**deck_pagesと同じ23個のカラム**を追加

### 管理者サイトでの対応

- deck_pagesと同じ

### 備考

- info_pagesとdeck_pagesは同じスキーマ構造
- 将来的にテーブル統合または用途別最適化を検討

---

## 5. info_articles（記事）

### 追加カラム（7個）

| 新カラム名 | 型 | 既存カラム | 説明 |
|-----------|-----|----------|------|
| slug_multilingual | JSONB | slug | URLスラッグ（各言語別） |
| title_multilingual | JSONB | title | タイトル |
| subtitle_multilingual | JSONB | subtitle | サブタイトル |
| excerpt_multilingual | JSONB | excerpt | 抜粋 |
| thumbnail_image_url_multilingual | JSONB | thumbnail_image_url | サムネイル画像URL |
| hero_image_url_multilingual | JSONB | hero_image_url | ヒーロー画像URL |
| tags_multilingual | JSONB | tags | タグ（配列） |

### 管理者サイトでの対応

- ✅ 作成・更新時に全言語で入力または自動翻訳
- ✅ slugは言語別に設定（SEO対策）

### インデックス

- `idx_info_articles_title_multilingual` (GIN)
- `idx_info_articles_slug_multilingual` (GIN)
- `idx_info_articles_tags_multilingual` (GIN)

### 多言語化不要

- ❌ `category` - ENUM
- ❌ `is_published`, `published_at`, `pinned`, `priority` - メタデータ
- ❌ `view_count`, `like_count`, `favorite_count`, `comment_count` - 統計
- ❌ `author_id` - 参照

---

## 6. info_article_blocks（記事ブロック）

### 追加カラム（1個）

| 新カラム名 | 型 | 既存カラム | 説明 |
|-----------|-----|----------|------|
| data_multilingual | JSONB | data | ブロックデータ（全タイプ・全言語） |

### 管理者サイトでの対応

- ✅ 各ブロックタイプごとにdata内のテキストを全言語で翻訳

### data構造の多言語化（全19種類のブロックタイプ）

ソースコード（lib/actions/info-articles.ts）から判明した全ブロックタイプを網羅的に記載します。

#### 1. heading（見出し）

**既存構造**:
```json
{
  "level": 1,
  "text": "水ポケモン大量発生イベントの攻略と報酬",
  "anchorId": "section-1"
}
```

**多言語化後**:
```json
{
  "ja": {
    "level": 1,
    "text": "水ポケモン大量発生イベントの攻略と報酬",
    "anchorId": "section-1"
  },
  "en": {
    "level": 1,
    "text": "Water Pokémon Mass Outbreak Event Guide and Rewards",
    "anchorId": "section-1"
  }
}
```

**翻訳対象**: `text`, `anchorId`（オプション）  
**翻訳不要**: `level`

---

#### 2. paragraph（段落）

**既存構造**:
```json
{
  "text": "最終更新：2025年9月28日 12:20"
}
```

**多言語化後**:
```json
{
  "ja": {"text": "最終更新：2025年9月28日 12:20"},
  "en": {"text": "Last updated: September 28, 2025 12:20"}
}
```

**翻訳対象**: `text`  
**翻訳不要**: なし

---

#### 3. image（画像）

**既存構造**:
```json
{
  "url": "https://kidyrurtyvxqokhszgko.supabase.co/storage/.../image.png",
  "alt": "水ポケモン大量発生イベントの攻略",
  "caption": "水ポケモン大量発生イベントの攻略",
  "aspect": "16:9"
}
```

**多言語化後**:
```json
{
  "ja": {
    "url": "https://.../image.png",
    "alt": "水ポケモン大量発生イベントの攻略",
    "caption": "水ポケモン大量発生イベントの攻略",
    "aspect": "16:9"
  },
  "en": {
    "url": "https://.../image-en.png",
    "alt": "Water Pokémon Mass Outbreak Event Guide",
    "caption": "Water Pokémon Mass Outbreak Event Guide",
    "aspect": "16:9"
  }
}
```

**翻訳対象**: `alt`, `caption`, `url`（言語別画像がある場合）  
**翻訳不要**: `aspect`

---

#### 4. list（リスト）

**既存構造**:
```json
{
  "style": "bulleted",
  "items": ["項目1", "項目2", "項目3"]
}
```

**多言語化後**:
```json
{
  "ja": {"style": "bulleted", "items": ["項目1", "項目2", "項目3"]},
  "en": {"style": "bulleted", "items": ["Item 1", "Item 2", "Item 3"]}
}
```

**翻訳対象**: `items`（配列の各要素）  
**翻訳不要**: `style` (`"bulleted"` or `"numbered"`)

---

#### 5. table（テーブル）

**既存構造**:
```json
{
  "headers": ["列1", "列2", "列3"],
  "rows": [
    ["データ1-1", "データ1-2", "データ1-3"],
    ["データ2-1", "データ2-2", "データ2-3"]
  ]
}
```

**多言語化後**:
```json
{
  "ja": {
    "headers": ["列1", "列2", "列3"],
    "rows": [
      ["データ1-1", "データ1-2", "データ1-3"],
      ["データ2-1", "データ2-2", "データ2-3"]
    ]
  },
  "en": {
    "headers": ["Column 1", "Column 2", "Column 3"],
    "rows": [
      ["Data 1-1", "Data 1-2", "Data 1-3"],
      ["Data 2-1", "Data 2-2", "Data 2-3"]
    ]
  }
}
```

**翻訳対象**: `headers[]`, `rows[][]`（全要素）  
**翻訳不要**: なし

---

#### 6. toc（目次）

**既存構造**:
```json
{
  "items": [
    {"label": "はじめに", "href": "#intro"},
    {"label": "使い方", "href": "#usage"}
  ]
}
```

**多言語化後**:
```json
{
  "ja": {
    "items": [
      {"label": "はじめに", "href": "#intro"},
      {"label": "使い方", "href": "#usage"}
    ]
  },
  "en": {
    "items": [
      {"label": "Introduction", "href": "#intro"},
      {"label": "How to Use", "href": "#usage"}
    ]
  }
}
```

**翻訳対象**: `items[].label`  
**翻訳不要**: `items[].href`

---

#### 7. related-links（関連リンク）

**既存構造**:
```json
{
  "items": [
    {"href": "/decks/123", "label": "関連デッキ"},
    {"href": "/articles/456", "label": "関連記事"}
  ]
}
```

**多言語化後**:
```json
{
  "ja": {
    "items": [
      {"href": "/decks/123", "label": "関連デッキ"},
      {"href": "/articles/456", "label": "関連記事"}
    ]
  },
  "en": {
    "items": [
      {"href": "/decks/123", "label": "Related Deck"},
      {"href": "/articles/456", "label": "Related Article"}
    ]
  }
}
```

**翻訳対象**: `items[].label`  
**翻訳不要**: `items[].href`

---

#### 8. divider（区切り線）

**既存構造**:
```json
{}
```

**多言語化**: 不要（空オブジェクト）

---

#### 9. callout（コールアウト）

**既存構造**:
```json
{
  "tone": "warning",
  "text": "この機能は実験的です",
  "title": "注意"
}
```

**多言語化後**:
```json
{
  "ja": {
    "tone": "warning",
    "text": "この機能は実験的です",
    "title": "注意"
  },
  "en": {
    "tone": "warning",
    "text": "This feature is experimental",
    "title": "Warning"
  }
}
```

**翻訳対象**: `text`, `title`  
**翻訳不要**: `tone` (`"info"` / `"warning"` / `"success"`)

---

#### 10. evaluation（評価）

**既存構造**:
```json
{
  "tier_rank": "S",
  "max_damage": 200,
  "build_difficulty": "入手しやすい",
  "stat_accessibility": "高い",
  "stat_stability": "安定",
  "eval_value": 4.5,
  "eval_count": 120
}
```

**多言語化後**:
```json
{
  "ja": {
    "tier_rank": "S",
    "max_damage": 200,
    "build_difficulty": "入手しやすい",
    "stat_accessibility": "高い",
    "stat_stability": "安定",
    "eval_value": 4.5,
    "eval_count": 120
  },
  "en": {
    "tier_rank": "S",
    "max_damage": 200,
    "build_difficulty": "Easy to obtain",
    "stat_accessibility": "High",
    "stat_stability": "Stable",
    "eval_value": 4.5,
    "eval_count": 120
  }
}
```

**翻訳対象**: `build_difficulty`, `stat_accessibility`, `stat_stability`  
**翻訳不要**: `tier_rank`, `max_damage`, `eval_value`, `eval_count`（数値）

---

#### 11. cards-table（カードテーブル）

**既存構造**:
```json
{
  "headers": {
    "id": "ID",
    "card": "カード名",
    "explanation": "説明",
    "quantity": "枚数"
  },
  "items": [
    {
      "id": "1",
      "card_id": 4762,
      "name": "ピカチュウex",
      "explanation": "メインアタッカー",
      "quantity": 2
    }
  ]
}
```

**多言語化後**:
```json
{
  "ja": {
    "headers": {
      "id": "ID",
      "card": "カード名",
      "explanation": "説明",
      "quantity": "枚数"
    },
    "items": [
      {
        "id": "1",
        "card_id": 4762,
        "name": "ピカチュウex",
        "explanation": "メインアタッカー",
        "quantity": 2
      }
    ]
  },
  "en": {
    "headers": {
      "id": "ID",
      "card": "Card Name",
      "explanation": "Description",
      "quantity": "Quantity"
    },
    "items": [
      {
        "id": "1",
        "card_id": 4762,
        "name": "Pikachu ex",
        "explanation": "Main Attacker",
        "quantity": 2
      }
    ]
  }
}
```

**翻訳対象**: `headers.*`, `items[].name`, `items[].explanation`  
**翻訳不要**: `items[].id`, `items[].card_id`, `items[].quantity`

---

#### 12. card-display-table（カード表示テーブル）

**既存構造**:
```json
{
  "rows": [
    {
      "id": "row-1",
      "header": "レアチャレンジ限定",
      "cards": [
        {
          "id": "4762",
          "name": "スイクンex",
          "imageUrl": "/placeholder.svg"
        }
      ]
    }
  ]
}
```

**多言語化後**:
```json
{
  "ja": {
    "rows": [
      {
        "id": "row-1",
        "header": "レアチャレンジ限定",
        "cards": [
          {"id": "4762", "name": "スイクンex", "imageUrl": "/placeholder.svg"}
        ]
      }
    ]
  },
  "en": {
    "rows": [
      {
        "id": "row-1",
        "header": "Rare Challenge Exclusive",
        "cards": [
          {"id": "4762", "name": "Suicune ex", "imageUrl": "/placeholder.svg"}
        ]
      }
    ]
  }
}
```

**翻訳対象**: `rows[].header`, `rows[].cards[].name`  
**翻訳不要**: `rows[].id`, `rows[].cards[].id`, `rows[].cards[].imageUrl`

---

#### 13. pickup（ピックアップ情報）

**既存構造**:
```json
{
  "title": "おすすめ情報",
  "items": [
    {"label": "新パック情報", "href": "/packs/new"},
    {"label": "イベント攻略", "href": "/events"}
  ]
}
```

**多言語化後**:
```json
{
  "ja": {
    "title": "おすすめ情報",
    "items": [
      {"label": "新パック情報", "href": "/packs/new"},
      {"label": "イベント攻略", "href": "/events"}
    ]
  },
  "en": {
    "title": "Featured Information",
    "items": [
      {"label": "New Pack Info", "href": "/packs/new"},
      {"label": "Event Guide", "href": "/events"}
    ]
  }
}
```

**翻訳対象**: `title`, `items[].label`  
**翻訳不要**: `items[].href`

---

#### 14. button（ボタン）

**既存構造**:
```json
{
  "label": "詳細を見る",
  "href": "/details"
}
```

**多言語化後**:
```json
{
  "ja": {"label": "詳細を見る", "href": "/details"},
  "en": {"label": "View Details", "href": "/details"}
}
```

**翻訳対象**: `label`  
**翻訳不要**: `href`

---

#### 15. key-value-table（キーバリューテーブル）

**既存構造**（実データ）:
```json
{
  "rows": [
    {
      "id": "row-1759037032050",
      "key": "開催期間",
      "valueType": "text",
      "textValue": "9月25日(木)15:00〜10月1日(水)14:59"
    },
    {
      "id": "row-1759037047154",
      "key": "報酬",
      "valueType": "card",
      "cardValues": [
        {"id": "4758", "name": "フリージオ", "imageUrl": "/placeholder.svg"}
      ]
    }
  ]
}
```

**多言語化後**:
```json
{
  "ja": {
    "rows": [
      {
        "id": "row-1759037032050",
        "key": "開催期間",
        "valueType": "text",
        "textValue": "9月25日(木)15:00〜10月1日(水)14:59"
      },
      {
        "id": "row-1759037047154",
        "key": "報酬",
        "valueType": "card",
        "cardValues": [
          {"id": "4758", "name": "フリージオ", "imageUrl": "/placeholder.svg"}
        ]
      }
    ]
  },
  "en": {
    "rows": [
      {
        "id": "row-1759037032050",
        "key": "Event Period",
        "valueType": "text",
        "textValue": "Thu Sep 25, 3:00 PM - Wed Oct 1, 2:59 PM"
      },
      {
        "id": "row-1759037047154",
        "key": "Rewards",
        "valueType": "card",
        "cardValues": [
          {"id": "4758", "name": "Cryogonal", "imageUrl": "/placeholder.svg"}
        ]
      }
    ]
  }
}
```

**翻訳対象**: `rows[].key`, `rows[].textValue`, `rows[].cardValues[].name`  
**翻訳不要**: `rows[].id`, `rows[].valueType`, `rows[].cardValues[].id`, `rows[].cardValues[].imageUrl`

---

#### 16. flexible-table（フレキシブルテーブル）

**既存構造**:
```json
{
  "title": "デッキ比較表",
  "style": "striped",
  "columns": [
    {"id": "col-1", "header": "デッキ名", "type": "text", "width": "200px"},
    {"id": "col-2", "header": "勝率", "type": "text", "width": "100px"}
  ],
  "rows": [
    {
      "id": "row-1",
      "cells": {
        "col-1": "ピカチュウデッキ",
        "col-2": "75%"
      }
    }
  ]
}
```

**多言語化後**:
```json
{
  "ja": {
    "title": "デッキ比較表",
    "style": "striped",
    "columns": [
      {"id": "col-1", "header": "デッキ名", "type": "text", "width": "200px"},
      {"id": "col-2", "header": "勝率", "type": "text", "width": "100px"}
    ],
    "rows": [
      {
        "id": "row-1",
        "cells": {
          "col-1": "ピカチュウデッキ",
          "col-2": "75%"
        }
      }
    ]
  },
  "en": {
    "title": "Deck Comparison Table",
    "style": "striped",
    "columns": [
      {"id": "col-1", "header": "Deck Name", "type": "text", "width": "200px"},
      {"id": "col-2", "header": "Win Rate", "type": "text", "width": "100px"}
    ],
    "rows": [
      {
        "id": "row-1",
        "cells": {
          "col-1": "Pikachu Deck",
          "col-2": "75%"
        }
      }
    ]
  }
}
```

**翻訳対象**: `title`, `columns[].header`, `rows[].cells[columnId]`（テキストセルのみ）  
**翻訳不要**: `style`, `columns[].id`, `columns[].type`, `columns[].width`, `rows[].id`

---

#### 17. media-gallery（メディアギャラリー）

**既存構造**:
```json
{
  "title": "ギャラリー",
  "layout": "grid",
  "items": [
    {
      "id": "img-1",
      "url": "https://.../image1.png",
      "caption": "画像1の説明",
      "alt": "画像1"
    }
  ]
}
```

**多言語化後**:
```json
{
  "ja": {
    "title": "ギャラリー",
    "layout": "grid",
    "items": [
      {
        "id": "img-1",
        "url": "https://.../image1.png",
        "caption": "画像1の説明",
        "alt": "画像1"
      }
    ]
  },
  "en": {
    "title": "Gallery",
    "layout": "grid",
    "items": [
      {
        "id": "img-1",
        "url": "https://.../image1-en.png",
        "caption": "Description of image 1",
        "alt": "Image 1"
      }
    ]
  }
}
```

**翻訳対象**: `title`, `items[].caption`, `items[].alt`, `items[].url`（言語別画像がある場合）  
**翻訳不要**: `layout`, `items[].id`

---

#### 18. rich-text（リッチテキスト）

**既存構造**:
```json
{
  "content": "<p>これはHTMLコンテンツです</p>",
  "format": "html",
  "style": {
    "fontSize": "16px",
    "color": "#333",
    "backgroundColor": "#f5f5f5",
    "textAlign": "center"
  }
}
```

**多言語化後**:
```json
{
  "ja": {
    "content": "<p>これはHTMLコンテンツです</p>",
    "format": "html",
    "style": {
      "fontSize": "16px",
      "color": "#333",
      "backgroundColor": "#f5f5f5",
      "textAlign": "center"
    }
  },
  "en": {
    "content": "<p>This is HTML content</p>",
    "format": "html",
    "style": {
      "fontSize": "16px",
      "color": "#333",
      "backgroundColor": "#f5f5f5",
      "textAlign": "center"
    }
  }
}
```

**翻訳対象**: `content`  
**翻訳不要**: `format` (`"html"` / `"markdown"` / `"plain"`), `style.*`

---

#### 19. latest-info（最新情報）

**既存構造**:
```json
{
  "title": "最新情報",
  "items": [
    {"label": "新機能リリース", "href": "/news/1"},
    {"label": "メンテナンス情報", "href": "/news/2"}
  ]
}
```

**多言語化後**:
```json
{
  "ja": {
    "title": "最新情報",
    "items": [
      {"label": "新機能リリース", "href": "/news/1"},
      {"label": "メンテナンス情報", "href": "/news/2"}
    ]
  },
  "en": {
    "title": "Latest News",
    "items": [
      {"label": "New Feature Release", "href": "/news/1"},
      {"label": "Maintenance Info", "href": "/news/2"}
    ]
  }
}
```

**翻訳対象**: `title`, `items[].label`  
**翻訳不要**: `items[].href`

### インデックス

- `idx_info_article_blocks_data_multilingual` (GIN)

### 多言語化不要

- ❌ `article_id`, `display_order`, `type` - メタデータ

---

## 7. tournaments（大会情報）

### 追加カラム（2個）

| 新カラム名 | 型 | 既存カラム | 説明 |
|-----------|-----|----------|------|
| title_multilingual | JSONB | title | 大会タイトル |
| benefit_multilingual | JSONB | benefit | 特典 |

### 管理者サイトでの対応

- ✅ 作成・更新時に全言語で入力または自動翻訳

### インデックス

- `idx_tournaments_title_multilingual` (GIN)

---

## 8. deck_notifications（デッキ通知）

### 追加カラム（1個）

| 新カラム名 | 型 | 既存カラム | 説明 |
|-----------|-----|----------|------|
| content_multilingual | JSONB | content | 通知内容（全言語） |

### 実装方法

通知作成時にテンプレートから全言語版を自動生成:

```typescript
const templates = {
  comment: {
    ja: "{{user}}さんがコメントしました",
    en: "{{user}} commented",
    "zh-CN": "{{user}}评论了",
    "zh-TW": "{{user}}評論了",
    ko: "{{user}}님이 댓글을 남겼습니다"
  },
  like: {
    ja: "{{user}}さんがいいねしました",
    en: "{{user}} liked",
    // ...
  }
};
```

### インデックス

- `idx_deck_notifications_content_multilingual` (GIN)

---

## 9. trade_notifications（トレード通知）

### 追加カラム（1個）

| 新カラム名 | 型 | 既存カラム | 説明 |
|-----------|-----|----------|------|
| content_multilingual | JSONB | content | 通知内容（全言語） |

### 実装方法

deck_notificationsと同様にテンプレート方式で全言語生成

### インデックス

- `idx_trade_notifications_content_multilingual` (GIN)

---

## 10. decks（ユーザー作成デッキ）

### 追加カラム（4個）

| 新カラム名 | 型 | NOT NULL | デフォルト | 説明 |
|-----------|-----|---------|-----------|------|
| title_multilingual | JSONB | ❌ | NULL | デッキタイトル（全言語） |
| description_multilingual | JSONB | ❌ | NULL | デッキ説明（全言語） |
| translation_status | TEXT | ❌ | 'pending' | 翻訳状態 |
| auto_translated_at | TIMESTAMPTZ | ❌ | NULL | 自動翻訳完了日時 |

### 翻訳方式

**バックグラウンド翻訳**:
1. ユーザーがデッキ作成/更新（日本語のみ入力）
2. `translation_jobs`テーブルにジョブ登録
3. バックグラウンドワーカーが順次処理して他言語に翻訳
4. 完了後`translation_status`を'completed'に更新

### 制約

```sql
ALTER TABLE decks
ADD CONSTRAINT check_translation_status
CHECK (translation_status IN ('pending', 'processing', 'completed', 'failed'));
```

### インデックス

- `idx_decks_title_multilingual` (GIN)
- `idx_decks_translation_status` (BTREE)

---

## 11. trade_posts（トレード投稿）

### 追加カラム（5個）

| 新カラム名 | 型 | NOT NULL | デフォルト | 説明 |
|-----------|-----|---------|-----------|------|
| title_multilingual | JSONB | ❌ | NULL | 投稿タイトル（全言語） |
| comment_multilingual | JSONB | ❌ | NULL | コメント（全言語） |
| wanted_card_id_multilingual | JSONB | ❌ | NULL | 求めるカード（全言語） |
| offered_card_id_multilingual | JSONB | ❌ | NULL | 譲れるカード（全言語） |
| translation_status | TEXT | ❌ | 'pending' | 翻訳状態 |

### 翻訳方式

バックグラウンド翻訳（decksと同じ）

### wanted_card_id / offered_card_id の多言語化（実データ構造）

**既存構造**（日本語のみ）:
```json
[
  {
    "id": 4795,
    "name": "リーリエ",
    "type": "",
    "rarity": "星2",
    "image_url": "https://kidyrurtyvxqokhszgko.supabase.co/storage/.../1801446.webp",
    "pack_name": 27
  },
  {
    "id": 3518,
    "name": "マーズ",
    "type": "",
    "rarity": "星2",
    "image_url": "https://kidyrurtyvxqokhszgko.supabase.co/storage/.../418.webp",
    "pack_name": 18
  }
]
```

**多言語化後**:
```json
{
  "ja": [
    {
      "id": 4795,
      "name": "リーリエ",
      "type": "",
      "rarity": "星2",
      "image_url": "https://.../1801446.webp",
      "pack_name": 27
    },
    {
      "id": 3518,
      "name": "マーズ",
      "type": "",
      "rarity": "星2",
      "image_url": "https://.../418.webp",
      "pack_name": 18
    }
  ],
  "en": [
    {
      "id": 4795,
      "name": "Lillie",
      "type": "",
      "rarity": "2 Star",
      "image_url": "https://.../1801446.webp",
      "pack_name": 27
    },
    {
      "id": 3518,
      "name": "Mars",
      "type": "",
      "rarity": "2 Star",
      "image_url": "https://.../418.webp",
      "pack_name": 18
    }
  ],
  "zh-CN": [
    {
      "id": 4795,
      "name": "莉莉艾",
      "type": "",
      "rarity": "2星",
      "image_url": "https://.../1801446.webp",
      "pack_name": 27
    },
    {
      "id": 3518,
      "name": "玛兹",
      "type": "",
      "rarity": "2星",
      "image_url": "https://.../418.webp",
      "pack_name": 18
    }
  ]
}
```

**翻訳対象キー**: `name`, `type`, `rarity`  
**翻訳不要キー**: `id`, `image_url`, `pack_name`（数値・URL）

**注意**: `pack_name`は現在数値IDですが、将来的にマスターデータ（packsテーブル）の`name_multilingual`から取得する設計に変更することを推奨

### インデックス

- `idx_trade_posts_title_multilingual` (GIN)
- `idx_trade_posts_translation_status` (BTREE)

---

## 12. trade_owned_list（トレードリスト）

### 追加カラム（2個）

| 新カラム名 | 型 | NOT NULL | デフォルト | 説明 |
|-----------|-----|---------|-----------|------|
| list_name_multilingual | JSONB | ❌ | NULL | リスト名（全言語） |
| translation_status | TEXT | ❌ | 'pending' | 翻訳状態 |

### 翻訳方式

バックグラウンド翻訳

### インデックス

- `idx_trade_owned_list_translation_status` (BTREE)

---

## 13. user_collages（ユーザーコラージュ）

### 追加カラム（3個）

| 新カラム名 | 型 | NOT NULL | デフォルト | 説明 |
|-----------|-----|---------|-----------|------|
| title1_multilingual | JSONB | ❌ | NULL | タイトル1（求めるカード・全言語） |
| title2_multilingual | JSONB | ❌ | NULL | タイトル2（譲れるカード・全言語） |
| translation_status | TEXT | ❌ | 'pending' | 翻訳状態 |

### 翻訳方式

バックグラウンド翻訳

### インデックス

- `idx_user_collages_translation_status` (BTREE)

---

## 14. trade_comments（トレードコメント）⚠️ 特殊対応

### 追加カラム

**なし - 都度翻訳方式を採用**

### 現状

- コメント数: **285,292件**
- 総文字数: **約700万文字**（更新）
- 全翻訳コスト: **$420（AWS Translate）**

### 採用方針: 都度翻訳（オンデマンド翻訳）

**データベースには翻訳を保存しない**

### 実装方法

#### オプション1: 翻訳ボタン方式
1. 各コメントに「翻訳」ボタンを表示
2. ユーザーがクリック時に`/api/translate-comment`呼び出し
3. APIは`translation_cache`を確認
   - キャッシュあり → すぐ返却
   - キャッシュなし → AWS Translateで翻訳 → キャッシュ保存 → 返却
4. 翻訳結果をクライアント側で一時表示

#### オプション2: ブラウザ標準翻訳
- Google Chrome等のブラウザ標準翻訳機能を利用
- 実装不要、コスト$0

### メリット

- ✅ 初期コスト: **$0**
- ✅ 実際に閲覧されるコメントのみ翻訳
- ✅ ストレージ節約（700万文字 × 4言語 = 2,800万文字の節約）
- ✅ translation_cacheで頻繁に閲覧されるコメントは再利用

---

## 15. users（ユーザー情報）

### 変更内容

**変更不要**

ユーザーの言語設定は別ドキュメント（`user-language-detection-options.md`）参照

---

## 新規テーブル

### translation_cache（翻訳キャッシュ）

```sql
CREATE TABLE translation_cache (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    source_text TEXT NOT NULL,
    source_language TEXT NOT NULL,
    target_language TEXT NOT NULL,
    translated_text TEXT NOT NULL,
    service_used TEXT NOT NULL, -- 'aws', 'deepl', 'google'
    char_count INTEGER NOT NULL,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    last_accessed_at TIMESTAMPTZ DEFAULT NOW(),
    access_count INTEGER DEFAULT 1,
    UNIQUE(source_text, source_language, target_language)
);

CREATE INDEX idx_translation_cache_lookup
ON translation_cache (source_text, source_language, target_language);

CREATE INDEX idx_translation_cache_last_accessed
ON translation_cache (last_accessed_at);
```

**用途**:
- 翻訳結果をキャッシュしてコスト削減
- 特にtrade_commentsの都度翻訳で効果的
- 定期的に古いキャッシュ（3ヶ月以上アクセスなし）を削除

---

### translation_jobs（翻訳ジョブ）

```sql
CREATE TABLE translation_jobs (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    entity_type TEXT NOT NULL, -- 'deck', 'trade_post', 'trade_owned_list', 'user_collage'
    entity_id TEXT NOT NULL,
    source_language TEXT NOT NULL,
    target_languages TEXT[] NOT NULL,
    status TEXT NOT NULL DEFAULT 'pending',
    priority INTEGER DEFAULT 0,
    service TEXT DEFAULT 'aws',
    error_message TEXT,
    retry_count INTEGER DEFAULT 0,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    started_at TIMESTAMPTZ,
    completed_at TIMESTAMPTZ,
    UNIQUE(entity_type, entity_id, status)
);

CREATE INDEX idx_translation_jobs_status
ON translation_jobs (status, priority DESC, created_at);

CREATE INDEX idx_translation_jobs_entity
ON translation_jobs (entity_type, entity_id);
```

**用途**:
- ユーザー生成コンテンツのバックグラウンド翻訳管理
- ワーカーがこのテーブルをポーリングして順次処理

---

## サマリー統計

### カラム追加数

| カテゴリ | テーブル数 | 多言語カラム総数 |
|---------|----------|--------------|
| マスターデータ | 2 | 3 |
| 管理者コンテンツ | 5 | 56 |
| 通知システム | 2 | 2 |
| ユーザーコンテンツ | 4 | 13 + 4 status |
| **合計** | **13** | **78** |

### インデックス追加数

- **GINインデックス**: 約60個
- **BTREEインデックス**: 約8個
- **合計**: 約68個

### 翻訳方式別

| 翻訳方式 | テーブル数 | 説明 |
|---------|----------|------|
| 管理者作成時・全自動翻訳 | 7 | cards, packs, deck_pages, info_pages, info_articles, info_article_blocks, tournaments |
| テンプレート自動生成 | 2 | deck_notifications, trade_notifications |
| バックグラウンド翻訳 | 4 | decks, trade_posts, trade_owned_list, user_collages |
| 都度翻訳（オンデマンド） | 1 | trade_comments |
| 変更不要 | 1 | users |

---

## 実装優先順位

### Phase 1: マスターデータ（1-2日）
1. ✅ cards - 管理画面で全言語入力対応
2. ✅ packs - 同上

### Phase 2: 管理者コンテンツ（2週間）
3. ✅ deck_pages - 23カラム対応、JSONB内翻訳
4. ✅ info_pages - 同上
5. ✅ info_articles - 7カラム対応
6. ✅ info_article_blocks - data_multilingual対応
7. ✅ tournaments - 2カラム対応

### Phase 3: 通知システム（2-3日）
8. ✅ deck_notifications - テンプレート実装
9. ✅ trade_notifications - 同上

### Phase 4: ユーザーコンテンツ（1週間）
10. ✅ translation_cache（新規）
11. ✅ translation_jobs（新規）
12. ✅ decks - バックグラウンド翻訳
13. ✅ trade_posts - 同上
14. ✅ trade_owned_list - 同上
15. ✅ user_collages - 同上

### Phase 5: 特殊対応（3-4日）
16. ✅ trade_comments - 都度翻訳UI実装

---

## 見積もり

### 開発工数

| Phase | 工数 | 主な作業 |
|-------|------|---------|
| Phase 1 | 2日 | DB変更 + 管理画面修正 |
| Phase 2 | 10日 | DB変更 + 管理画面大幅修正 + JSONBエディタ |
| Phase 3 | 2日 | テンプレートシステム実装 |
| Phase 4 | 7日 | バックグラウンドワーカー実装 |
| Phase 5 | 3日 | フロントエンド翻訳ボタン実装 |
| **合計** | **24日** | |

### 初期翻訳コスト

| 項目 | 件数 | 文字数 | 翻訳文字数（×4言語） | AWS Cost |
|------|------|--------|-------------------|----------|
| cards | 2,000 | 20,000 | 80,000 | $1.20 |
| packs | 50 | 1,500 | 6,000 | $0.09 |
| deck_pages | 200 | 500,000 | 2,000,000 | $30.00 |
| info_pages | 200 | 500,000 | 2,000,000 | $30.00 |
| info_articles | 100 | 150,000 | 600,000 | $9.00 |
| tournaments | 50 | 5,000 | 20,000 | $0.30 |
| **管理者コンテンツ合計** | - | **1,176,500** | **4,706,000** | **$70.59** |
| decks（人気上位1000） | 1,000 | 100,000 | 400,000 | $6.00 |
| trade_posts（最新1000） | 1,000 | 150,000 | 600,000 | $9.00 |
| **ユーザーコンテンツ** | - | **250,000** | **1,000,000** | **$15.00** |
| trade_comments | - | - | - | **$0（都度翻訳）** |
| **総合計** | - | **1,426,500** | **5,706,000** | **$85.59** |

### 月間ランニングコスト

| 項目 | 月間文字数 | 翻訳文字数 | AWS Cost |
|------|-----------|-----------|----------|
| 新規管理者コンテンツ | 30,000 | 120,000 | $1.80 |
| 新規ユーザーコンテンツ | 50,000 | 200,000 | $3.00 |
| trade_comments翻訳 | 10,000 | 40,000 | $0.60 |
| **月額合計** | **90,000** | **360,000** | **$5.40** |

**年間コスト**: 約 $65

---

## 実装チェックリスト

### データベース
- [ ] 全テーブルのALTER TABLE実行（78カラム追加）
- [ ] インデックス作成（68個）
- [ ] 制約追加（CHECK制約）
- [ ] 既存データの初期変換（日本語のみ）
- [ ] translation_cache, translation_jobs作成

### 管理画面
- [ ] cards: 全言語入力フォーム + 自動翻訳ボタン
- [ ] packs: 同上
- [ ] deck_pages: 23フィールド全言語対応 + JSONBエディタ
- [ ] info_pages: 同上
- [ ] info_articles: 7フィールド対応
- [ ] info_article_blocks: ブロック別多言語エディタ
- [ ] tournaments: 2フィールド対応
- [ ] 翻訳プレビュー機能

### バックエンド
- [ ] AWS Translate API統合
- [ ] 翻訳APIエンドポイント（/api/translate）
- [ ] バックグラウンドワーカー実装
- [ ] translation_cache CRUD
- [ ] translation_jobs管理
- [ ] 通知テンプレートシステム

### フロントエンド
- [ ] 多言語テキスト取得ヘルパー関数
- [ ] 言語切り替え機能
- [ ] trade_comments翻訳ボタン実装
- [ ] ローディング・エラー表示
- [ ] JSONBデータの多言語対応

### テスト
- [ ] 単体テスト（ヘルパー関数）
- [ ] 統合テスト（翻訳API）
- [ ] E2Eテスト（言語切り替え）
- [ ] 負荷テスト（翻訳API同時リクエスト）
- [ ] JSONB構造のバリデーション

### モニタリング
- [ ] 翻訳APIコスト追跡ダッシュボード
- [ ] translation_jobs処理状況モニター
- [ ] エラー通知（Slack/Email）
- [ ] パフォーマンス監視（レスポンスタイム）

---

## 補足事項

### stat_*カラムの多言語化について

`stat_accessibility`, `stat_speed`等は数値ですが、UIで表示する際のラベルや説明文があるため多言語化対象としました。

**データ構造例**:
```json
{
  "ja": {
    "value": 3,
    "label": "入手しやすい",
    "description": "基本パックのカードで構成されており、無課金でも組める"
  },
  "en": {
    "value": 3,
    "label": "Easy to get",
    "description": "Composed of basic pack cards, buildable without spending"
  }
}
```

### is_published, view_countについて

これらは多言語化不要です（誤記を修正）。

### JSONB構造の翻訳精度

deck_cardsやstrengths_weaknesses_detailsなど複雑なJSONB構造は、各フィールドを個別に翻訳する必要があるため、管理画面で専用エディタを実装することを推奨します。

---

## 全JSONB構造の翻訳対象キーまとめ

このセクションでは、全テーブルのJSONBカラムについて、どのキーが翻訳対象か網羅的にまとめます。

### deck_pages & info_pages

| JSONBカラム | 翻訳対象キー | 翻訳不要キー | データ型 |
|-----------|------------|------------|---------|
| deck_cards | `pack_name` | `card_id`, `card_count`, `display_order` | Array of Objects |
| strengths_weaknesses_details | `title`, `description` | `image_urls`, `display_order` | Array of Objects |
| how_to_play_list | 全ての値（配列要素） | - | JSONB key-value object |
| how_to_play_steps | （未使用または将来実装） | - | TBD |
| stat_accessibility | `label`, `description` | `value` | Object |
| stat_speed | `label`, `description` | `value` | Object |
| stat_power | `label`, `description` | `value` | Object |
| stat_durability | `label`, `description` | `value` | Object |
| stat_stability | `label`, `description` | `value` | Object |

### info_article_blocks

| ブロックtype | 翻訳対象キー | 翻訳不要キー |
|------------|------------|------------|
| heading | `text` | `level` |
| paragraph | `text` | - |
| image | `alt`, `caption`, `url`（言語別画像の場合） | `aspect` |
| key-value-table | `rows[].key`, `rows[].textValue`, `rows[].cardValues[].name` | `rows[].id`, `rows[].valueType`, `rows[].cardValues[].id`, `rows[].cardValues[].imageUrl` |
| list | `items[]`（各要素） | `style` |
| table | `headers[]`, `rows[][]` | - |

### trade_posts

| JSONBカラム | 翻訳対象キー | 翻訳不要キー |
|-----------|------------|------------|
| wanted_card_id | `name`, `type`, `rarity` | `id`, `image_url`, `pack_name` |
| offered_card_id | `name`, `type`, `rarity` | `id`, `image_url`, `pack_name` |

### trade_matches

| JSONBカラム | データ型 | 備考 |
|-----------|---------|------|
| initiator_cards | JSONB | 実データなし - 空のまま |
| responder_cards | JSONB | 実データなし - 空のまま |
| wanted_cards | JSONB | 実データなし - 空のまま |

**注**: trade_matchesのJSONBカラムは現在使用されていないため、多言語化の優先度は低い。将来的にtrade_postsと同じ構造で使用される場合は同様に対応。

### deck_lists

| JSONBカラム | データ型 | 備考 |
|-----------|---------|------|
| query | JSONB | 実データなし - フィルター・ソート条件を格納予定 |

**注**: queryカラムは検索条件を保存するためのものと推測され、翻訳不要の可能性が高い。

---

## JSONBカラム翻訳実装の注意点

### 1. ネストされたJSONB構造の翻訳

deck_cards、strengths_weaknesses_details等のように配列内にオブジェクトがネストしている場合:

**誤った実装例**（全体を翻訳してしまう）:
```typescript
// ❌ NG - オブジェクト全体を翻訳文字列として扱ってしまう
const translated = await translate(JSON.stringify(deckCards));
```

**正しい実装例**（キーごとに翻訳）:
```typescript
// ✅ OK - 翻訳対象キーのみを個別に翻訳
const translatedDeckCards = await Promise.all(
  deckCards.map(async (card) => ({
    ...card,
    pack_name: await translate(card.pack_name, targetLang)
  }))
);
```

### 2. 管理画面での専用エディタ実装

複雑なJSONB構造（特にinfo_article_blocksのkey-value-table）は、通常のテキストエディタでは編集困難なため、ブロックタイプ別の専用エディタを実装することを強く推奨:

- heading: シンプルなテキスト入力 + レベル選択
- paragraph: リッチテキストエディタ
- image: 画像アップロード + alt/caption入力
- key-value-table: 動的な行追加/削除UI + カード検索機能
- list: 動的なリスト項目追加/削除UI

### 3. 翻訳キャッシュの活用

同じカード名（例: "ピカチュウex"）が複数のtrade_postsやdeck_cardsに出現する場合、translation_cacheで再利用することでコスト削減:

```typescript
// カード名の翻訳をキャッシュから取得
const cachedTranslation = await getCachedTranslation("ピカチュウex", "ja", "en");
if (cachedTranslation) {
  return cachedTranslation;
}
```

---

**このドキュメントは最終確定版です。実装開始前の最終確認用としてご利用ください。**
