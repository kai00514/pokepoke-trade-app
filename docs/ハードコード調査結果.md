# ハードコード調査結果レポート

**生成日時:** 2025年11月30日
**調査対象:** 「要確認」ステータスの38件

---

## 📊 調査結果サマリー

| 項目 | 件数 | 割合 |
|------|------|------|
| **ハードコード発見** | 23件 | 60.5% |
| **既に修正済み（false positive）** | 10件 | 26.3% |
| **その他** | 5件 | 13.2% |

---

## 🔴 ハードコード発見項目（23件）

### 1. ナビゲーション関連

| キー | 英語 | 日本語（ハードコード） | 発見箇所 |
|------|------|----------------------|----------|
| `common.navigation.backToTimeline` | Back to Timeline | タイムラインに戻る | `app/[locale]/collages/page.tsx`, `app/[locale]/contact/page.tsx`, `app/[locale]/lists/page.tsx` |

**修正方針:**
```typescript
// 現在
<Link href="/">タイムラインに戻る</Link>

// 修正後
<Link href="/">{t('common.navigation.backToTimeline')}</Link>
```

---

### 2. ボタン関連（最優先）

| キー | 英語 | 日本語（ハードコード） | 発見箇所 |
|------|------|----------------------|----------|
| `common.buttons.complete` | Complete | 選択完了 | `components/admin/deck-card-selection-modal.tsx` |
| `common.buttons.close` | Close | 閉じる | `components/admin/deck-card-selection-modal.tsx`, `components/notification-dropdown.tsx` |
| `common.buttons.creating` | Creating... | 作成中... | `app/[locale]/lists/create/page.tsx` |
| `common.buttons.cancel` | Cancel | キャンセル | `app/[locale]/lists/[id]/page.tsx`, `app/[locale]/page.tsx`, `app/admin/tournaments/page.tsx` |
| `common.buttons.next` | Next | 次へ | `components/admin/article-table.tsx`, `components/news-carousel.tsx` |
| `common.buttons.back` | Back | 戻る | `app/[locale]/collages/[id]/collage-page-client.tsx`, `app/[locale]/collages/page.tsx`, `app/[locale]/contact/page.tsx` |
| `common.buttons.generate` | Generate Collage | コラージュ画像を生成 | `components/collage/collage-list.tsx` |
| `common.buttons.save` | Save | 保存 | `app/[locale]/decks/[id]/page.tsx`, `app/[locale]/decks/create/page.tsx` |
| `common.buttons.delete` | Delete | 削除 | `app/[locale]/lists/[id]/page.tsx`, `app/[locale]/decks/[id]/page.tsx` |
| `common.buttons.edit` | Edit | 編集 | `app/[locale]/decks/[id]/page.tsx`, `app/[locale]/lists/[id]/page.tsx` |

**修正方針:**
```typescript
// 現在
<Button>選択完了</Button>

// 修正後
<Button>{t('common.buttons.complete')}</Button>
```

---

### 3. ラベル・その他UI

| キー | 英語 | 日本語（ハードコード） | 発見箇所 |
|------|------|----------------------|----------|
| `common.labels.user` | User | ユーザー | `app/[locale]/decks/[id]/page.tsx`, `app/[locale]/favorites/page.tsx`, `app/[locale]/lists/create/page.tsx` |
| `common.misc.notSelected` | No Card Selected | カード未選択 | `components/admin/blocks/key-value-table-block-editor.tsx` |
| `common.notifications.title` | Notifications | 通知 | `app/[locale]/lists/create/page.tsx`, `components/deck-card.tsx`, `components/header.tsx` |
| `common.notifications.markAsRead` | Read | 既読 | `components/notification-dropdown.tsx` |
| `common.notifications.loading` | Loading... | 読み込み中... | `app/[locale]/auth/login/loading.tsx`, `app/[locale]/content/[id]/page.tsx`, `app/[locale]/decks/[id]/page.tsx` |

---

### 4. メッセージ関連

| キー | 英語 | 日本語（ハードコード） | 発見箇所 |
|------|------|----------------------|----------|
| `messages.success.copied` | Copied to clipboard | クリップボードにコピーしました | `app/[locale]/decks/create/page.tsx`, `app/[locale]/decks/[id]/page.tsx` |
| `messages.success.saved` | Saved successfully | 保存しました | `app/[locale]/decks/[id]/page.tsx`, `app/[locale]/decks/create/page.tsx` |
| `messages.info.selectCards` | Select at least one card | カードを1枚以上選択してください | `components/admin/deck-card-selection-modal.tsx` |
| `messages.info.loadMore` | Load more | さらに読み込む | `components/admin/article-table.tsx` |

---

## ✅ 既に修正済み（10件）

以下は「部分一致」で検出されましたが、実際には正しく翻訳キーを使用しています：

- `common.buttons.continueAsGuest` - login-prompt.tsxで既に`t('common.buttons.continueAsGuest')`使用
- `common.buttons.generating` - 実際のハードコードは見つからず
- `common.notifications.markAllAsRead` - notification-dropdown.tsxで既に`t('messages.notifications.markAllAsRead')`使用
- `common.notifications.noNotifications` - notification-dropdown.tsxで既に使用
- `common.notifications.unknownUser` - notification-dropdown.tsxで既に使用
- `common.notifications.unknownDetails` - notification-dropdown.tsxで既に使用
- `messages.success.listCreated` - false positive
- `messages.success.deckCreated` - false positive
- その他2件

---

## 🎯 修正優先度

### 🔥 最優先（高頻度使用）

1. **ボタンテキスト** - 10件
   - `common.buttons.save`, `cancel`, `complete`, `close` など
   - 影響範囲: 複数ページ・コンポーネント

2. **ナビゲーション** - 1件
   - `common.navigation.backToTimeline`
   - 影響範囲: collages, contact, listsページ

### 🟡 高優先

3. **通知関連** - 3件
   - `common.notifications.*`
   - 影響範囲: 通知システム全体

4. **ラベル** - 2件
   - `common.labels.user`, `common.misc.notSelected`

### 🔵 中優先

5. **メッセージ** - 7件
   - `messages.success.*`, `messages.info.*`

---

## 📝 修正が必要なファイル一覧

### 最多修正が必要（4箇所以上）

1. **components/admin/deck-card-selection-modal.tsx** - 4箇所
   - `選択完了`, `閉じる`, `カード未選択`, `カードを1枚以上選択してください`

2. **app/[locale]/decks/create/page.tsx** - 3箇所
   - `保存`, `クリップボードにコピーしました`, `保存しました`

3. **app/[locale]/lists/create/page.tsx** - 2箇所
   - `作成中...`, `通知`

### 複数箇所の修正が必要（2-3箇所）

4. **app/[locale]/collages/[id]/collage-page-client.tsx** - 2箇所
5. **app/[locale]/decks/[id]/page.tsx** - 2箇所
6. **app/[locale]/lists/[id]/page.tsx** - 2箇所
7. **app/[locale]/contact/page.tsx** - 2箇所
8. **app/[locale]/content/[id]/page.tsx** - 2箇所

### その他（1箇所）

9. components/notification-dropdown.tsx
10. components/collage/collage-list.tsx
11. components/admin/article-table.tsx
12. components/news-carousel.tsx
13. その他複数ファイル

---

## 🔧 修正手順

### ステップ1: 最優先ファイルから修正

```bash
# 1. deck-card-selection-modal.tsx (4箇所)
# 2. decks/create/page.tsx (3箇所)
# 3. lists/create/page.tsx (2箇所)
```

### ステップ2: 修正パターン

```typescript
// Before: ハードコード
<Button>選択完了</Button>
<Button>保存</Button>
<p>読み込み中...</p>

// After: i18n対応
import { useTranslations } from 'next-intl'

const t = useTranslations()

<Button>{t('common.buttons.complete')}</Button>
<Button>{t('common.buttons.save')}</Button>
<p>{t('common.notifications.loading')}</p>
```

### ステップ3: CSVファイルを更新

修正完了後、該当行のStatusを「完了」に更新：

```csv
"common.buttons.complete","Complete","common.json","components/admin/deck-card-selection-modal.tsx","...","完了","修正完了: ハードコード→i18n"
```

---

## 📊 期待される効果

| 項目 | 現在 | 修正後 | 改善 |
|------|------|--------|------|
| 完了率 | 109件 (21.0%) | 132件 (25.4%) | +23件 (+4.4%) |
| 要確認 | 38件 (7.3%) | 15件 (2.9%) | -23件 (-4.4%) |

---

## 📋 次のアクション

1. ✅ このレポートを確認
2. 🔴 最優先ファイルから修正開始
   - components/admin/deck-card-selection-modal.tsx
   - app/[locale]/decks/create/page.tsx
   - app/[locale]/lists/create/page.tsx
3. 🟡 CSVファイルのステータスを随時更新
4. 🔵 全修正完了後、進捗レポートを再生成

---

**調査ツール:** Node.js + grep による自動調査
**調査データ:** `/tmp/hardcoded_investigation.csv`
