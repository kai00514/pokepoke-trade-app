# 要確認項目の詳細分析レポート

**生成日時:** 2025年11月30日
**対象:** 部分一致で検出された78件の翻訳キー

---

## 📋 概要

「要確認」とマークされた78件の翻訳キーは、**名前空間プレフィックスなしで呼び出されている**ことが判明しました。

### 問題のパターン

```typescript
// ❌ 現在（名前空間なし）
const t = useTranslations()
t('categories.all')

// ✅ 修正後（名前空間あり）
const t = useTranslations()
t('cards.categories.all')
```

---

## 🔍 コンポーネント別詳細

### 1. detailed-search-modal.tsx（48件）

**問題:** `cards.*` 名前空間のキーを名前空間なしで呼び出し

#### カテゴリー関連（5件）
```typescript
// 現在
t("categories.all")
t("categories.pokemon")
t("categories.trainers")
t("categories.goods")
t("categories.tools")

// 修正後
t("cards.categories.all")
t("cards.categories.pokemon")
t("cards.categories.trainers")
t("cards.categories.goods")
t("cards.categories.tools")
```

#### タイプ関連（11件）
```typescript
// 現在
t("types.all")
t("types.grass")
t("types.fire")
t("types.water")
t("types.electric")
t("types.psychic")
t("types.fighting")
t("types.darkness")
t("types.metal")
t("types.colorless")
t("types.dragon")

// 修正後
t("cards.types.all")
t("cards.types.grass")
// ... 以下同様
```

#### レアリティ関連（7件）
```typescript
// 現在
t("rarities.all")
t("rarities.diamond1")
t("rarities.diamond2")
t("rarities.diamond3")
t("rarities.diamond4")
t("rarities.star1")
t("rarities.star2")

// 修正後
t("cards.rarities.all")
t("cards.rarities.diamond1")
// ... 以下同様
```

**修正方針:**
- すべての `t("categories.*")` を `t("cards.categories.*")` に置換
- すべての `t("types.*")` を `t("cards.types.*")` に置換
- すべての `t("rarities.*")` を `t("cards.rarities.*")` に置換

---

### 2. notification-dropdown.tsx（12件）

**問題:** `messages.*` 名前空間のキーを名前空間なしで呼び出し

#### 通知関連
```typescript
// 現在
t('notifications.types.trade')
t('notifications.types.deck')
t('notifications.types.other')
t('notifications.time.now')
t('notifications.time.minutesAgo', { count: diffInMinutes })
t('notifications.time.hoursAgo', { count: diffInHours })
t('notifications.unknownUser')
t('notifications.unknownDetails')
t('notifications.unreadCount', { count: unreadCount })
t('notifications.title')
t('notifications.markAllAsRead')

// 修正後
t('messages.notifications.types.trade')
t('messages.notifications.types.deck')
// ... 以下同様
```

**修正方針:**
- すべての `t('notifications.*')` を `t('messages.notifications.*')` に置換

---

### 3. collage-generator-modal.tsx（12件）

**問題:** `forms.*` 名前空間のキーを名前空間なしで呼び出し

#### コラージュ関連
```typescript
// 現在
t("validation.enterTitle")
t("validation.selectAtLeastOneGroup")
t("collage.group1Title.placeholder")
t("collage.group2Title.placeholder")
t("collage.generationFailed")
t("modals.collage.title")
t("collage.group1Title.label")
t("collage.group2Title.label")

// 修正後（validationはforms、collageとmodalsはpagesかcommon）
t("forms.validation.enterTitle")
t("forms.validation.selectAtLeastOneGroup")
t("pages.collage.group1Title.placeholder")  // または forms.collage.*
// ... 確認が必要
```

**修正方針:**
- 翻訳ファイルを確認して、正しい名前空間を特定
- `validation.*` は `forms.*` 名前空間の可能性が高い
- `collage.*` と `modals.*` は確認が必要

---

### 4. trade-owned-lists/list-creation-modal.tsx（数件）

**問題:** `common.buttons.*` を名前空間なしで呼び出し

```typescript
// 現在（推測）
t("creating")
t("close")
t("complete")

// 修正後
t("common.buttons.creating")
t("common.buttons.close")
t("common.buttons.complete")
```

---

### 5. login-prompt.tsx（2件）

**問題:** `auth.*` 名前空間を部分的に使用

```typescript
// 現在（推測）
t("loginPrompt.title")
t("loginPrompt.description")

// 修正後
t("auth.loginPrompt.title")
t("auth.loginPrompt.description")
```

---

### 6. trade-detail-client.tsx & trade-post-card.tsx（数件）

**問題:** `common.navigation.*` を名前空間なしで呼び出し

```typescript
// 現在（推測）
t("navigation.backToTimeline")

// 修正後
t("common.navigation.backToTimeline")
```

---

## 🎯 修正の優先順位

### 🔥 最優先（高頻度使用）

1. **detailed-search-modal.tsx** - 48件
   - カード検索モーダルは頻繁に使用される
   - カテゴリー、タイプ、レアリティの表示が正しく翻訳されない

2. **notification-dropdown.tsx** - 12件
   - 通知機能は全ページで使用される
   - 通知時刻やタイプの表示が英語化されない

### 🟡 高優先

3. **collage-generator-modal.tsx** - 12件
   - コラージュ生成機能
   - バリデーションメッセージが日本語のまま

4. **trade-owned-lists/list-creation-modal.tsx**
   - リスト作成モーダル
   - ボタンラベルが日本語のまま

### 🔵 中優先

5. **login-prompt.tsx**
6. **trade-detail-client.tsx**
7. **trade-post-card.tsx**

---

## 🛠️ 修正方法

### ステップ1: 翻訳ファイルを確認

まず、各キーがどの翻訳ファイルに定義されているか確認：

```bash
# categories, types, rarities は cards.json に存在
grep "categories" locales/en/cards.json

# notifications は messages.json に存在
grep "notifications" locales/en/messages.json
```

### ステップ2: コンポーネントを一括置換

```typescript
// detailed-search-modal.tsx の修正例

// Before
const categories = [
  t("categories.all"),
  t("categories.pokemon"),
  // ...
]

// After
const categories = [
  t("cards.categories.all"),
  t("cards.categories.pokemon"),
  // ...
]
```

### ステップ3: CSVファイルのステータスを更新

修正後、該当行のStatusを「完了」に変更：

```csv
"cards.categories.all","All","cards.json","components/detailed-search-modal.tsx","detailed-search-modal","","完了","修正完了: 名前空間追加"
```

---

## 📊 修正後の期待効果

| 項目 | 修正前 | 修正後 | 改善 |
|------|--------|--------|------|
| 完了率 | 13.2% (69/519) | 28.3% (147/519) | +15.1% |
| 要確認 | 15.0% (78/519) | 0.0% (0/519) | -15.0% |
| cards名前空間 | 0.0% | 100% | +100% |
| messages名前空間 | 0.0% | 26.7% | +26.7% |

---

## ✅ チェックリスト

- [ ] detailed-search-modal.tsx の修正（48件）
  - [ ] categories.* → cards.categories.*
  - [ ] types.* → cards.types.*
  - [ ] rarities.* → cards.rarities.*

- [ ] notification-dropdown.tsx の修正（12件）
  - [ ] notifications.* → messages.notifications.*

- [ ] collage-generator-modal.tsx の修正（12件）
  - [ ] 翻訳ファイルの確認
  - [ ] 正しい名前空間への置換

- [ ] trade-owned-lists/list-creation-modal.tsx の修正
  - [ ] ボタンラベルの名前空間追加

- [ ] その他のコンポーネント修正
  - [ ] login-prompt.tsx
  - [ ] trade-detail-client.tsx
  - [ ] trade-post-card.tsx

- [ ] CSVファイルのステータス更新
  - [ ] 修正完了項目を「完了」に変更
  - [ ] Notesに修正内容を記録

- [ ] 動作確認
  - [ ] /en でカード検索モーダルを開いて英語表示を確認
  - [ ] /en で通知ドロップダウンを確認
  - [ ] /en でコラージュ生成モーダルを確認

---

**次のアクション:**
1. `detailed-search-modal.tsx` の修正から開始（最も影響が大きい）
2. 各修正後にCSVファイルのStatusを更新
3. 全修正完了後、進捗レポートを再生成
